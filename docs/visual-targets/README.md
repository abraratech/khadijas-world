# Visual target references

These images are **concept references, not Babylon.js screenshots**. They define composition, warmth, character proportions and UI direction. The benchmark scene determines the exact production quality that the minimum machine can sustain.

The project must not promise pixel-for-pixel parity with these generated references until equivalent assets have been rendered and measured in-engine.
