{
  "name": "khadijas-world",
  "private": true,
  "version": "0.25.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/build-pwa.mjs",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "test": "vitest run --config vitest.unit.config.ts",
    "deploy:wamp": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/deploy-wamp.ps1",
    "validate:assets": "node scripts/validate-assets.mjs",
    "check:bundle": "node scripts/check-bundle-size.mjs",
    "build:verified": "npm run build && npm run check:bundle && npm run pwa:check",
    "check:qa": "tsc --noEmit -p tsconfig.qa.json",
    "qa:install": "playwright install chromium",
    "qa:browser": "playwright test",
    "qa:perf": "playwright test tests/browser/performance.spec.ts",
    "qa:browser:headed": "playwright test --headed",
    "qa:browser:ui": "playwright test --ui",
    "qa:verified": "npm run validate:assets && npm run check && npm test && npm run check:qa && npm run qa:browser && npm run check:bundle && npm run pwa:check",
    "build:cloudflare": "tsc --noEmit && vite build --base / && node scripts/build-pwa.mjs",
    "chat:dev": "npm run build:cloudflare && npx wrangler@latest pages dev dist --ai AI",
    "cloud-save:dev": "npm run build:cloudflare && npx wrangler@latest pages dev dist --config wrangler.jsonc",
    "cloud-save:migrate:local": "npx wrangler@latest d1 migrations apply khadijas-world-saves --local --config wrangler.jsonc",
    "cloud-save:migrate:remote": "npx wrangler@latest d1 migrations apply khadijas-world-saves --remote --config wrangler.jsonc",
    "pwa:check": "node scripts/check-pwa.mjs",
    "release:check": "node scripts/check-release.mjs",
    "release:mobile": "playwright test tests/browser/release-mobile.spec.ts --workers=1",
    "release:verified": "npm run validate:assets && npm run check && npm test && npm run check:qa && npm run build:cloudflare && npm run pwa:check && npm run check:bundle && npm run release:check"
  },
  "dependencies": {
    "@babylonjs/core": "9.18.0",
    "@babylonjs/loaders": "9.18.0"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@types/node": "^26.1.2",
    "typescript": "^5.9.2",
    "vite": "8.1.5",
    "vitest": "^4.1.10"
  }
}
